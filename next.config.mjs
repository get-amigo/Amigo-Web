/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: "",
  images: {
    domains: ['aceternity.com'], // Add the domain here
  },
};

export default nextConfig;
